<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/accentColor</item>

        <item name="android:textColor">@color/primaryTextColor</item>

        <item name="windowNoTitle">true</item>
    </style>

    <style name="DashletTheme" parent="AppTheme">
        <item name="android:layout_margin">@dimen/dashlet_margin</item>
        <item name="android:padding">@dimen/dashlet_padding</item>
        <item name="cardBackgroundColor">@color/primaryColor</item>
    </style>

    <style name="TitleAppearance" parent="@style/TextAppearance.MaterialComponents.Body1">
        <item name="color">@color/secondaryTextColor</item>
    </style>

    <style name="MessageAppearance" parent="@style/TextAppearance.MaterialComponents.Headline6">
        <item name="color">@color/primaryTextColor</item>
    </style>

</resources>
